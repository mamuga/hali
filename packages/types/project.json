{
  "name": "types",
  "sourceRoot": "packages/types/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p packages/types/tsconfig.json --noEmit"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p packages/types/tsconfig.json"
      }
    }
  },
  "tags": ["scope:shared", "type:types"]
}
